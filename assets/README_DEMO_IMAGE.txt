This folder should contain the demo screenshot shown in the README at `assets/demo.png`.

Please replace this placeholder with the actual PNG screenshot file named `demo.png`.

To add the image locally and commit:

1. Copy the PNG into this folder and name it `demo.png`.
2. Run:

   git add assets/demo.png
   git commit -m "docs: add demo screenshot"
   git push origin master

If you'd like me to add the screenshot from the chat attachment, re-upload the original `demo.png` file so I can place it in `assets/demo.png` and commit it for you.
